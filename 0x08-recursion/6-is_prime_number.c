#include "main.h"

/**
 * is_prime_number - checks if number is prime number
 * @n - the number we checking if its a prime number
 * Return : integer
 */

int is_prime_number(int n)
{
if ( n % 2 == 0)
{
return (0);
}
else
{
return (1);
}
}
