#include "main.h";

/**
 * _strlen_recursion - prints a reversed string,
 * @s: string to check
 * Return: void
 */

void _strlen_recursion(*s)
{
if (*s == '\0')
{
return (0);
}
else
{
return (1 + string_length(str + 1));
}
}
